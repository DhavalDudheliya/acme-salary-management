{
  "name": "supporthub",
  "version": "1.0.0",
  "private": true,
  "description": "SupportHub - Full stack application",
  "scripts": {
    "prepare": "husky"
  },
  "engines": {
    "node": ">=24.13.1"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^16.2.7",
    "prettier": "^3.8.1"
  },
  "lint-staged": {
    "backend/**/*.{ts,tsx}": [
      "prettier --write"
    ],
    "frontend/**/*.{ts,tsx,js,jsx}": [
      "prettier --write"
    ]
  }
}
